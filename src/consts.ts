export const INJECT_RETRY_COUNT = 10;
export const INJECT_RETRY_PERIOD_MS = 200;

export const SPEED_PANEL_CLASS = 'enhanced-speed-panel';

export const SPEED_SLD_STEP_SIZE = 0.05;
export const SPEED_KEY_STEP_SIZE = 0.25;
export const SPEED_MIN = 0.05;
export const SPEED_MAX = 8;

export const MICRO_TOAST_TIMEOUT_MS = 1000;

export const NUM_FORMATTER = new Intl.NumberFormat(undefined, {
  minimumFractionDigits: 1, maximumFractionDigits: 2
});
